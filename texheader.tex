% removes indentation from abstract
\usepackage{etoolbox}
\ifundef{\abstract}{}{\patchcmd{\abstract}
    {\quotation}{\quotation\noindent\ignorespaces}{}{}}

% adds line numbering
\usepackage[right]{lineno}
\linenumbers
\setlength{\linenumbersep}{20pt} %moves numbers slightly further (default is 10pt)

% adds endnotes (uncomment the two lines below then follow next directions)
% \usepackage{endnotes}
% \let\footnote=\endnote

% to use endnotes, add the following before the #Works Cited portion of bib-header.md
% \begingroup
% \parindent 0pt
% \parskip 2ex
% \def\enotesize{\normalsize}
% \theendnotes
% \endgroup